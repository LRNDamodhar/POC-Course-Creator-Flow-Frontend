export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  // Base URL for the AI Course Creator server (proxied via /ai in dev to avoid CORS)
  aiccUrl: 'https://authoring.qa4.lrn.com'
};
